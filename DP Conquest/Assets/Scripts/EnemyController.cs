using UnityEngine;
using System.Collections;

/*
 * Base class for enemies that can the player can collide with or can collide with the player 
 * 
 */

public class EnemyController : MonoBehaviour {

	/*
	 * Called on the frame of the object’s creation and overrides the MonoBehaviour.Start() method. 
	 * Currently does nothing but functionality may be included later.
	 */
	private void Start () {
	
	}
	
	/*
	 * An update function inherited from Unity’s MonoBehaviour class that is called 
	 * every frame the game runs. 
	 * Currently does nothing but may be overridden in the future.
	 */
	private void Update () {
	
	}
}
