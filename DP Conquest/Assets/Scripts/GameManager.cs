using UnityEngine;
using System.Collections;

/*
 * Instantiates the player’s character and the main game sequence
 * 
 */

public class GameManager : MonoBehaviour {


	/* Called on the frame of the object’s creation and overrides the MonoBehaviour.Start() method.
	 * Currently does nothing but functionality may be included later.
	 */
	private void Start () {

	}

	/* An update function inherited from Unity’s MonoBehaviour class that is called every 
	 * frame the game runs. 
	 * Currently does nothing but may be overridden in the future.
	 */
	//void Update(){}
}
